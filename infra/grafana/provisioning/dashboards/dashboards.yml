apiVersion: 1

providers:
  - name: verisettle
    orgId: 1
    folder: VeriSettle
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
